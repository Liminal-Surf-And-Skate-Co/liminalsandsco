// Stubbed: @lovable.dev/cloud-auth-js is not available in this environment.
// This module is not imported by any route or component (verified via grep).
export const lovable = {
  auth: {
    signInWithOAuth: async () => ({ error: new Error("Lovable auth not available") }),
  },
};
